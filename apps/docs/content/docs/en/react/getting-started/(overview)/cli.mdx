---
title: CLI
description: Use the CLI to manage SY INC dependencies and initialize projects.
icon: terminal
---

The CLI offers a comprehensive suite of commands to initialize, manage, and improve your SY INC projects. It enables you to `install`, `uninstall`, or `upgrade` individual components, assess the health of your project, download documentation for AI coding agents, and more.

## Installation

Requirements:

- [Node.js version 22.22.0 or later](https://nodejs.org/en/)


### Global Installation

To install `sy-inc-cli` globally, execute one of the following commands in your terminal:


```npm
npm install sy-inc-cli@latest -g
```

### Without Installation

Alternatively, you can use `sy-inc-cli` without a global installation by running one of the following:

<Tabs items={["pnpm", "npm", "yarn", "bun"]}>
  <Tab value="pnpm">
    ```bash
    pnpm dlx sy-inc-cli@latest
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npx sy-inc-cli@latest
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn dlx sy-inc-cli@latest
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bunx sy-inc-cli@latest
    ```
  </Tab>
</Tabs>

## Quick Start

Once `sy-inc-cli` is installed, run the following command to display available commands:

```bash
sy-inc
```

This will produce the following help output:

```bash
Usage: sy-inc [command]

Options:
  -v, --version                  Output the current version
  --no-cache                     Disable cache, by default data will be cached for 30m after the first request
  -d, --debug                    Debug mode will not install dependencies
  -h --help                      Display help information for commands

Commands:
  init [options] [projectName]   Initializes a new project
  install [options]              Installs @sy-inc/react and @sy-inc/styles to your project
  upgrade [options]              Upgrades @sy-inc/react and @sy-inc/styles to the latest versions
  uninstall [options]            Uninstall @sy-inc/react and @sy-inc/styles from the project
  list [options]                 Lists installed SY INC packages (@sy-inc/react, @sy-inc/styles)
  env [options]                  Displays debugging information for the local environment
  doctor [options]               Checks for issues in the project
  agents-md [options]            Downloads SY INC documentation for AI coding agents
  help [command]                 Display help for command
```

### init

Initialize a new SY INC project using the `init` command. This sets up your project with the necessary configurations.

```bash
sy-inc init [options]
```

**Options:**

- `-t --template [string]` The template to use for the new project e.g. app, pages, vite, react-router
- `-p --package [string]` The package manager to use for the new project

output:

```bash
SY INC CLI <version>

┌  Create a new project
│
◇  Select a template (Enter to select)
│  ● App (A Next.js 16 with app directory template pre-configured with SY INC (v3) and Tailwind CSS.)
│  ○ Pages (A Next.js 16 with pages directory template pre-configured with SY INC (v3) and Tailwind CSS.)
│  ○ Vite (A Vite template pre-configured with SY INC (v3) and Tailwind CSS.)
│  ○ React Router (A React Router template pre-configured with SY INC (v3) and Tailwind CSS.)
│
◇  New project name (Enter to skip with default name)
│  my-sy-inc-app
│
◇  Select a package manager (Enter to select)
│  ● npm
│  ○ yarn
│  ○ pnpm
│  ○ bun
│
◇  Template created successfully!
│
◇  Next steps ───────╮
│                    │
│  cd my-sy-inc-app  │
│  npm install       │
│                    │
├────────────────────╯
│
└  🚀 Get started with npm run dev
```


Install the dependencies to start the local server:

<Tabs items={["npm", "pnpm", "yarn", "bun"]}>
  <Tab value="npm">
    ```bash
    cd my-sy-inc-app && npm install
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    cd my-sy-inc-app && pnpm install
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    cd my-sy-inc-app && yarn install
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    cd my-sy-inc-app && bun install
    ```
  </Tab>
</Tabs>

Start the local server:

```npm
npm run dev
```

### Install

Install `@sy-inc/react` and `@sy-inc/styles` to your project, along with their peer dependencies. If they are already installed, the command does nothing.

```bash
sy-inc install [options]
```

**Options:**

- `-p --packagePath` [string] The path to the package.json file


**Output:**

```bash
SY INC CLI <version>

📦 Packages to be installed:
╭─────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version        │   Status   │   Docs                 │
│─────────────────────────────────────────────────────────────────────────────│
│   @sy-inc/react    │   3.0.0          │   stable   │   https://sy-inc.com   │
│   @sy-inc/styles   │   3.0.0          │   stable   │   https://sy-inc.com   │
╰─────────────────────────────────────────────────────────────────────────────╯

╭─────────────── PeerDependencies ────────────────╮
│  react@18.3.1                      latest       │
│  react-dom@18.3.1                  latest       │
│  tailwindcss@4.2.2                 latest       │
╰─────────────────────────────────────────────────╯
? Proceed with installation? › - Use arrow-keys. Return to submit.
❯   Yes
    No

✅ @sy-inc/react and @sy-inc/styles installed successfully
```

### upgrade


Upgrade `@sy-inc/react` and `@sy-inc/styles` with their peer dependencies to the latest versions.

```bash
sy-inc upgrade [options]
```

**Options:**

- `-p --packagePath` [string] The path to the package.json file

**Output:**

```bash
SY INC CLI <version>

╭──────────────────────────── Upgrade ────────────────────────────╮
│  @sy-inc/react               ^3.0.0  ->  ^3.1.0                │
│  @sy-inc/styles              ^3.0.0  ->  ^3.1.0                │
╰─────────────────────────────────────────────────────────────────╯

? Would you like to proceed with the upgrade? › - Use arrow-keys. Return to submit.
❯   Yes
    No

✅ Upgrade complete. All packages are up to date.
```

### uninstall

Uninstall `@sy-inc/react` and `@sy-inc/styles` from your project. Peer dependencies will not be uninstalled.

```bash
sy-inc uninstall [options]
```

**Options:**

- `-p --packagePath` [string] The path to the package.json file

**Output:**

```bash
SY INC CLI <version>

❗️ Packages slated for uninstallation:
╭──────────────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version   │   Status   │   Docs                               │
│──────────────────────────────────────────────────────────────────────────────────────│
│   @sy-inc/react    │   3.0.0     │   stable   │   https://sy-inc.com                 │
│   @sy-inc/styles   │   3.0.0     │   stable   │   https://sy-inc.com                 │
╰──────────────────────────────────────────────────────────────────────────────────────╯
? Confirm uninstallation of these packages: › - Use arrow-keys. Return to submit.
❯   Yes
    No

✅ Successfully uninstalled: @sy-inc/react, @sy-inc/styles
```

### list

List the installed SY INC packages (`@sy-inc/react`, `@sy-inc/styles`).

```bash
sy-inc list [options]
```

**Options:**

- `-p --packagePath` [string] The path to the package.json file

**Output:**

```bash
SY INC CLI <version>

Current installed packages:

╭──────────────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version          │   Status   │   Docs                        │
│──────────────────────────────────────────────────────────────────────────────────────│
│   @sy-inc/react    │   3.0.0 🚀latest   │   stable   │   https://sy-inc.com          │
│   @sy-inc/styles   │   3.0.0 🚀latest   │   stable   │   https://sy-inc.com          │
╰──────────────────────────────────────────────────────────────────────────────────────╯
```

### doctor

Check for issues in your project.
- Check whether `@sy-inc/react` and `@sy-inc/styles` are installed
- Check whether `required peer dependencies` are installed and matched minimal requirements in the project


```bash
sy-inc doctor [options]
```

**Options:**

- `-p --packagePath` [string] The path to the package.json file

**Output:**

If there is a problem in your project, the `doctor` command will display the problem information.

```bash
SY INC CLI <version>

SY INC CLI: ❌ Your project has 1 issue that require attention

❗️Issue 1: missingSyIncPackages

The following SY INC packages are not installed:
- @sy-inc/styles

Run `sy-inc install` to install them.
```

Otherwise, the `doctor` command will display the following message.

```bash
SY INC CLI <version>

✅ Your project has no detected issues.
```

### env

Display debug information about the local environment.

```bash
sy-inc env [options]
```

**Options:**

- `-p --packagePath` [string] The path to the package.json file

**Output:**

```bash
SY INC CLI <version>

Current installed packages:

╭──────────────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version          │   Status   │   Docs                        │
│──────────────────────────────────────────────────────────────────────────────────────│
│   @sy-inc/react    │   3.0.0 🚀latest   │   stable   │   https://sy-inc.com          │
│   @sy-inc/styles   │   3.0.0 🚀latest   │   stable   │   https://sy-inc.com          │
╰──────────────────────────────────────────────────────────────────────────────────────╯

Environment Info:
  System:
    OS: darwin
    CPU: arm64
  Binaries:
    Node: v25.8.1
```

### agents-md

Download SY INC documentation for AI coding agents (Claude, Cursor, etc.). The command clones the latest docs from the SY INC repository and injects a compact index into `AGENTS.md` or `CLAUDE.md` so assistants can reference your project's SY INC setup.

```bash
sy-inc agents-md [options]
```

**Options:**

- `--react` [boolean] Include React docs only (one library at a time)
- `--native` [boolean] Include Native docs only
- `--migration` [boolean] Include SY INC v2 to v3 migration docs only
- `--output <file>` [string] Target file path (e.g., `AGENTS.md`, `CLAUDE.md`)
- `--ssh` [boolean] Use SSH instead of HTTPS for git clone

**Examples:**

Run without flags to enter interactive mode:

```bash
sy-inc agents-md
```

Download React docs to a specific file:

```bash
sy-inc agents-md --react --output AGENTS.md
```

Download Native or migration docs:

```bash
sy-inc agents-md --native --output CLAUDE.md
sy-inc agents-md --migration --output AGENTS.md
```

**How it works:**

1. Clones documentation from the `v3` branch using git sparse-checkout
2. Generates a compact index of doc and demo files
3. Injects the index into your markdown file between markers (`<!-- SY-INC-REACT-AGENTS-MD-START -->` / `<!-- SY-INC-REACT-AGENTS-MD-END -->`, and similar for Native and Migration)
4. Adds `.sy-inc-docs/` to `.gitignore`

Only one of `--react`, `--native`, or `--migration` can be selected at a time.

For more details, see [AGENTS.md](/docs/react/getting-started/agents-md).

<Callout>
  The `agents-md` command collects anonymous usage data (selection, output file names, duration, success or error). Set `SY_INC_ANALYTICS_DISABLED=1` to opt out.
</Callout>

## Reporting issues

If you found a bug, please report it in [sy-inc-cli Issues](https://github.com/sy-inc/sy-inc-cli/issues).

