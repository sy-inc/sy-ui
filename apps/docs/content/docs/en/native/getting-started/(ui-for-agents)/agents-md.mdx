---
title: AGENTS.md
description: Download SY UI Native documentation for AI coding agents
icon: preview
---

Download SY UI Native documentation directly into your project for AI assistants to reference.

<Callout>
  **Note:** The `agents-md` command is specifically for SY UI React v3 and SY UI Native. Other CLI commands (like `add`, `init`, `upgrade`, etc.) are for SY UI v2 (for now).
</Callout>

<DocsImage
  src="https://assets.sy-ui.com/docs/agents-md-native.jpg"
  darkSrc="https://assets.sy-ui.com/docs/agents-md-native-dark.jpg"
  alt="SY UI Native AGENTS.md"
/>

### Usage

```bash
npx sy-ui-cli@latest agents-md --native
```

Or specify output file:

```bash
npx sy-ui-cli@latest agents-md --native --output AGENTS.md
```

### What It Does

- Downloads latest SY UI Native docs to `.sy-ui-docs/native/`
- Generates an index in `AGENTS.md` or `CLAUDE.md`
- Adds `.sy-ui-docs/` to `.gitignore` automatically

### Options

- `--native` - Download Native docs only
- `--output <files...>` - Target file(s) (e.g., `AGENTS.md` or `AGENTS.md CLAUDE.md`)
- `--ssh` - Use SSH for git clone

### Requirements

- Tailwind CSS >= v4 (via Uniwind)

### Related Documentation

- [AGENTS.md](https://agents.md/) - Learn about the AGENTS.md format for coding agents
- [CLAUDE.md](https://code.claude.com/docs/en/best-practices#write-an-effective-claude-md) - Claude equivalent of AGENTS.md
- [AGENTS.md vs Skills](https://vercel.com/blog/agents-md-outperforms-skills-in-our-agent-evals) - AGENTS.md performance
