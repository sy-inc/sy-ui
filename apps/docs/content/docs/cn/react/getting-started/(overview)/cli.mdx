---
title: 命令行
description: 使用命令行管理 SY UI 依赖并初始化项目。
icon: terminal
---

`sy-ui-cli`是官方命令行工具，提供了一整套命令，用于初始化、管理和优化你的 SY UI 项目。你可以 `install`、`uninstall` 或 `upgrade` 单个组件，检查项目健康状况，下载用于 AI 编程助手的文档，等等。

## 安装

环境要求：

- [Node.js 22.22.0 或更高版本](https://nodejs.org/en/)


### 全局安装

要全局安装 `sy-ui-cli`，请在终端中执行以下命令之一：


```npm
npm install sy-ui-cli@latest -g
```

### 无需安装直接使用

或者，你也可以在不进行全局安装的情况下使用 `sy-ui-cli`，运行以下命令之一即可：

<Tabs items={["pnpm", "npm", "yarn", "bun"]}>
  <Tab value="pnpm">
    ```bash
    pnpm dlx sy-ui-cli@latest
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npx sy-ui-cli@latest
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn dlx sy-ui-cli@latest
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bunx sy-ui-cli@latest
    ```
  </Tab>
</Tabs>

## 快速开始

安装 `sy-ui-cli` 后，运行以下命令以查看可用命令：

```bash
sy-ui
```

将会输出如下帮助信息：

```bash
Usage: sy-ui [command]

Options:
  -v, --version                  Output the current version
  --no-cache                     Disable cache, by default data will be cached for 30m after the first request
  -d, --debug                    Debug mode will not install dependencies
  -h --help                      Display help information for commands

Commands:
  init [options] [projectName]   Initializes a new project
  install [options]              Installs @sy-ui/react and @sy-ui/styles to your project
  upgrade [options]              Upgrades @sy-ui/react and @sy-ui/styles to the latest versions
  uninstall [options]            Uninstall @sy-ui/react and @sy-ui/styles from the project
  list [options]                 Lists installed SY UI packages (@sy-ui/react, @sy-ui/styles)
  env [options]                  Displays debugging information for the local environment
  doctor [options]               Checks for issues in the project
  agents-md [options]            Downloads SY UI documentation for AI coding agents
  help [command]                 Display help for command
```

### init

使用 `init` 命令初始化一个新的 SY UI 项目。该命令会为你的项目完成必要的配置。

```bash
sy-ui init [options]
```

**选项：**

- `-t --template [string]` 新项目使用的模板，例如 app、pages、vite、react-router
- `-p --package [string]` 新项目使用的包管理器

输出：

```bash
SY UI CLI <version>

┌  Create a new project
│
◇  Select a template (Enter to select)
│  ● App (A Next.js 16 with app directory template pre-configured with SY UI (v3) and Tailwind CSS.)
│  ○ Pages (A Next.js 16 with pages directory template pre-configured with SY UI (v3) and Tailwind CSS.)
│  ○ Vite (A Vite template pre-configured with SY UI (v3) and Tailwind CSS.)
│  ○ React Router (A React Router template pre-configured with SY UI (v3) and Tailwind CSS.)
│
◇  New project name (Enter to skip with default name)
│  my-sy-ui-app
│
◇  Select a package manager (Enter to select)
│  ● npm
│  ○ yarn
│  ○ pnpm
│  ○ bun
│
◇  Template created successfully!
│
◇  Next steps ───────╮
│                    │
│  cd my-sy-ui-app  │
│  npm install       │
│                    │
├────────────────────╯
│
└  🚀 Get started with npm run dev
```


安装依赖以启动本地服务器：

<Tabs items={["npm", "pnpm", "yarn", "bun"]}>
  <Tab value="npm">
    ```bash
    cd my-sy-ui-app && npm install
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    cd my-sy-ui-app && pnpm install
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    cd my-sy-ui-app && yarn install
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    cd my-sy-ui-app && bun install
    ```
  </Tab>
</Tabs>

启动本地服务器：

```npm
npm run dev
```

### Install

将 `@sy-ui/react` 和 `@sy-ui/styles` 及其对等依赖安装到你的项目中。若它们已安装，则该命令不会执行任何操作。

```bash
sy-ui install [options]
```

**选项：**

- `-p --packagePath` [string] package.json 文件的路径


**输出：**

```bash
SY UI CLI <version>

📦 Packages to be installed:
╭─────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version        │   Status   │   Docs                 │
│─────────────────────────────────────────────────────────────────────────────│
│   @sy-ui/react    │   3.0.0          │   stable   │   https://sy-ui.com   │
│   @sy-ui/styles   │   3.0.0          │   stable   │   https://sy-ui.com   │
╰─────────────────────────────────────────────────────────────────────────────╯

╭─────────────── PeerDependencies ────────────────╮
│  react@18.3.1                      latest       │
│  react-dom@18.3.1                  latest       │
│  tailwindcss@4.2.2                 latest       │
╰─────────────────────────────────────────────────╯
? Proceed with installation? › - Use arrow-keys. Return to submit.
❯   Yes
    No

✅ @sy-ui/react and @sy-ui/styles installed successfully
```

### upgrade


将 `@sy-ui/react` 和 `@sy-ui/styles` 及其对等依赖升级到最新版本。

```bash
sy-ui upgrade [options]
```

**选项：**

- `-p --packagePath` [string] package.json 文件的路径

**输出：**

```bash
SY UI CLI <version>

╭──────────────────────────── Upgrade ────────────────────────────╮
│  @sy-ui/react               ^3.0.0  ->  ^3.1.0                │
│  @sy-ui/styles              ^3.0.0  ->  ^3.1.0                │
╰─────────────────────────────────────────────────────────────────╯

? Would you like to proceed with the upgrade? › - Use arrow-keys. Return to submit.
❯   Yes
    No

✅ Upgrade complete. All packages are up to date.
```

### uninstall

从你的项目中卸载 `@sy-ui/react` 和 `@sy-ui/styles`。对等依赖不会被卸载。

```bash
sy-ui uninstall [options]
```

**选项：**

- `-p --packagePath` [string] package.json 文件的路径

**输出：**

```bash
SY UI CLI <version>

❗️ Packages slated for uninstallation:
╭──────────────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version   │   Status   │   Docs                               │
│──────────────────────────────────────────────────────────────────────────────────────│
│   @sy-ui/react    │   3.0.0     │   stable   │   https://sy-ui.com                 │
│   @sy-ui/styles   │   3.0.0     │   stable   │   https://sy-ui.com                 │
╰──────────────────────────────────────────────────────────────────────────────────────╯
? Confirm uninstallation of these packages: › - Use arrow-keys. Return to submit.
❯   Yes
    No

✅ Successfully uninstalled: @sy-ui/react, @sy-ui/styles
```

### list

列出已安装的 SY UI 包（`@sy-ui/react`、`@sy-ui/styles`）。

```bash
sy-ui list [options]
```

**选项：**

- `-p --packagePath` [string] package.json 文件的路径

**输出：**

```bash
SY UI CLI <version>

Current installed packages:

╭──────────────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version          │   Status   │   Docs                        │
│──────────────────────────────────────────────────────────────────────────────────────│
│   @sy-ui/react    │   3.0.0 🚀latest   │   stable   │   https://sy-ui.com          │
│   @sy-ui/styles   │   3.0.0 🚀latest   │   stable   │   https://sy-ui.com          │
╰──────────────────────────────────────────────────────────────────────────────────────╯
```

### doctor

检查项目中存在的问题。
- 检查 `@sy-ui/react` 和 `@sy-ui/styles` 是否已安装
- 检查项目中是否安装了所需的 `对等依赖` 并满足最低版本要求


```bash
sy-ui doctor [options]
```

**选项：**

- `-p --packagePath` [string] package.json 文件的路径

**输出：**

如果项目中存在问题，`doctor` 命令将显示问题信息。

```bash
SY UI CLI <version>

SY UI CLI: ❌ Your project has 1 issue that require attention

❗️Issue 1: missingSyUIPackages

The following SY UI packages are not installed:
- @sy-ui/styles

Run `sy-ui install` to install them.
```

否则，`doctor` 命令将显示以下消息。

```bash
SY UI CLI <version>

✅ Your project has no detected issues.
```

### env

显示本地环境的调试信息。

```bash
sy-ui env [options]
```

**选项：**

- `-p --packagePath` [string] package.json 文件的路径

**输出：**

```bash
SY UI CLI <version>

Current installed packages:

╭──────────────────────────────────────────────────────────────────────────────────────╮
│   Package          │   Version          │   Status   │   Docs                        │
│──────────────────────────────────────────────────────────────────────────────────────│
│   @sy-ui/react    │   3.0.0 🚀latest   │   stable   │   https://sy-ui.com          │
│   @sy-ui/styles   │   3.0.0 🚀latest   │   stable   │   https://sy-ui.com          │
╰──────────────────────────────────────────────────────────────────────────────────────╯

Environment Info:
  System:
    OS: darwin
    CPU: arm64
  Binaries:
    Node: v25.8.1
```

### agents-md

下载用于 AI 编程助手（Claude、Cursor 等）的 SY UI 文档。该命令会从 SY UI 仓库克隆最新文档，并将一份精简索引注入到 `AGENTS.md` 或 `CLAUDE.md` 中，方便助手参考你项目中的 SY UI 配置。

```bash
sy-ui agents-md [options]
```

**选项：**

- `--react` [boolean] 仅包含 React 文档（一次只能选择一个文档库）
- `--native` [boolean] 仅包含 Native 文档
- `--migration` [boolean] 仅包含 SY UI v2 到 v3 的迁移文档
- `--output <file>` [string] 目标文件路径（例如 `AGENTS.md`、`CLAUDE.md`）
- `--ssh` [boolean] 使用 SSH 而非 HTTPS 进行 git clone

**示例：**

不带任何标志运行以进入交互模式：

```bash
sy-ui agents-md
```

将 React 文档下载到指定文件：

```bash
sy-ui agents-md --react --output AGENTS.md
```

下载 Native 或迁移文档：

```bash
sy-ui agents-md --native --output CLAUDE.md
sy-ui agents-md --migration --output AGENTS.md
```

**工作原理：**

1. 使用 git sparse-checkout 从 `v3` 分支克隆文档
2. 生成文档和示例文件的精简索引
3. 将该索引注入到你的 markdown 文件中，置于标记之间（`<!-- SY-UI-REACT-AGENTS-MD-START -->` / `<!-- SY-UI-REACT-AGENTS-MD-END -->`，Native 和 Migration 也有类似的标记）
4. 将 `.sy-ui-docs/` 添加到 `.gitignore`

`--react`、`--native` 和 `--migration` 一次只能选择其中一个。

更多详情请参阅 [AGENTS.md](/docs/react/getting-started/agents-md)。

<Callout>
  `agents-md` 命令会收集匿名使用数据（所选项、输出文件名、耗时、成功或错误）。设置 `SY_UI_ANALYTICS_DISABLED=1` 可以选择不参与。
</Callout>

## 问题反馈

如果你发现了 bug，请在 [sy-ui-cli Issues](https://github.com/sy-ui/sy-ui-cli/issues) 中报告。

